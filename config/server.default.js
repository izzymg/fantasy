// Server configuration file

module.exports = {
    // Server host and port
    port: 3000,
    host: "localhost",
    https: true,
    httpsPort: 3001,
    // Server log file
    log: "/var/log/z.log",
    // Log exceptions out to console
    consoleErrors: true,
    // Used in rendering pages (e.g. name of your website/domain)
    webname: "ZChan",
};
