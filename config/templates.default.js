const prefix = "ZThree - ";

module.exports = {
    // <title> elements
    titles: {
        home: `${prefix}Home`,
        boards: `${prefix}Boards`,
        rules: `${prefix}Rules`,
        thread: `${prefix}`,
        catalog: `${prefix}Catalog`
    }
};