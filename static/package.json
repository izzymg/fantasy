{
  "name": "zthreestatic",
  "version": "1.0.0",
  "description": "Frontend",
  "scripts": {
    "build": "sass --style compressed --no-source-map src/css:dist/css",
    "watch": "sass --watch src/css:dist/css"
  },
  "dependencies": {
    "sass": "^1.16.1"
  }
}
